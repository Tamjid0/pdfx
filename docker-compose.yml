services:
  # Backend Server
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: pdfx-server
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - REDIS_HOST=pdfx-redis
      - REDIS_PORT=6379
      - NODE_ENV=production
    env_file:
      - ./server/.env
    volumes:
      - ./server/src:/app/src
      - ./server/uploads:/app/uploads
    depends_on:
      - redis
    networks:
      - pdfx-network

  # Redis for Background Jobs
  redis:
    image: redis:7.2-alpine
    container_name: pdfx-redis
    ports:
      - "6381:6379"
    volumes:
      - pdfx_redis_data_v4:/data
    restart: always
    networks:
      - pdfx-network

networks:
  pdfx-network:
    driver: bridge

volumes:
  pdfx_redis_data_v4:
